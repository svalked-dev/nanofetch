git clone https://github.com/svalked-dev/nanofetch.git && cd nanofetch && mkdir -p ~/.local/bin && cp mf.py art.txt ~/.local/bin/ && chmod +x ~/.local/bin/mf.py && echo 'alias nanofetch="cd ~/.local/bin && python3 mf.py"' >> ~/.bashrc && source ~/.bashrc && cd .. && rm -rf nanofetch && echo "start the nanofetch: nanofetch"
